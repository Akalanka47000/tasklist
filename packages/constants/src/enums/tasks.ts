export enum TaskStatus {
  Done = 'Done',
  NotDone = 'Not Done'
}

export enum Priority {
  Low = 1,
  Medium = 2,
  High = 3
}

export enum RecurringInterval {
  Daily = 'Daily',
  Weekly = 'Weekly',
  Monthly = 'Monthly'
}
